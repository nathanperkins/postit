<div class='row'>
  <div class='span8 well'>
    <h4><%= link_to user.username, user_path(user) %></h4>
    <p>
      <span class='quiet'>has </span><small><%= "#{pluralize(user.posts.size,'Post')}" %></small>
    </p>
    <span class='quiet'>joined </span><small><%= user.created_at %></small>
  </div>
</div>