<% if @vote.valid? %>
  $('#post_<%= @post.to_param %>_votes').html('<%= @post.total_votes %>');
<% else %>
  // alert('You can only vote on each post once.')
  $('#post_<%= @post.to_param %>_errors').show();
<% end %>
